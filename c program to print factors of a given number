//c program to print factors of a given number
#include<stdio.h>
main()
{
	int n,i=1;
	printf("enter n value");
	scanf("%d",&n);
	
	
	printf("Factors of a given number are %d:",n);
	
	while(i<=n)
	{
	if(n% i == 0)
	printf(" %d",i);
	i++;
    
  }
}
